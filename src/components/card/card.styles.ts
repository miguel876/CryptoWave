import { Card, styled } from '@mui/material';

export const StyledCard = styled(Card)`
  padding: 2rem;
  box-shadow: 0 0 0.2rem rgba(128, 138, 157, 0.12),
    0 0 2.4rem rgba(128, 138, 157, 0.14);
  min-height: 22.8rem;
`;
